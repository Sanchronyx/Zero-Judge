#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(void){
    string str;
    getline(cin, str);
    vector <int> num(str.size(), 0);
    for (auto it = str.begin(); it != str.end(); it++){
        if (it != str.end() - 1){
            
        }
    }
    return 0;
}